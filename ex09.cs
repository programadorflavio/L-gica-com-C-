using System;

class Program {
  public static void Main (string[] args) {
   Console.WriteLine("Qual é a distância do carro Y ?");
     int d = int.Parse(Console.ReadLine());
     int cal = d * 2;
    Console.WriteLine ("A distância entre o carro X e Y é de :" + cal + "km");
    
    
  }
}
